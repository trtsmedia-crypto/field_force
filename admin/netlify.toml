[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router handles the routes, so every path must serve index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
