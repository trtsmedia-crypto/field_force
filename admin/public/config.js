// Runtime configuration. Edit this file after deploying — no rebuild needed.

// Point this at your API. It must include /api/v1 at the end, and it must be
// https:// once your site is on https, or browsers will block the requests.
window.__FIELDFORCE_API__ = "http://localhost:4000/api/v1";

// Optional. Set this to a Google Maps JavaScript API key to show a real map
// on the Live Tracking page. Leave it empty to keep the schematic view —
// nothing breaks either way.
// Get a key at console.cloud.google.com/google/maps-apis, enable
// "Maps JavaScript API", and restrict it to your site's domain.
window.__FIELDFORCE_MAPS_KEY__ = "";
